{
  "name": "shared-sockets",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/src/index.js",
  "scripts": {
    "build": "tsc && echo built",
    "lint": "tslint ./tests/**/*.ts ./src/**/*.ts && echo linted",
    "test": "tsc && ava -t ./dist/tests/**/*.js | tap-spec"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  },
  "devDependencies": {
    "ava": "^0.16.0",
    "http-status": "^0.2.3",
    "supertest": "^2.0.0",
    "tap-spec": "^4.1.1",
    "tslint": "^3.15.1",
    "typescript": "^1.8.10"
  }
}
