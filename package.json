{
  "name": "river-styx",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/src/index.js",
  "scripts": {
    "build": "tsc && echo built",
    "lint": "tslint ./tests/**/*.ts ./src/**/*.ts && echo linted",
    "test": "tsc && tape ./dist/tests/**/*.js | tap-spec",
    "typings": "typings"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  },
  "devDependencies": {
    "http-status": "^0.2.3",
    "supertest": "^2.0.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.0",
    "tslint": "^3.15.1",
    "typescript": "^1.8.10",
    "typings": "^1.3.3"
  }
}
